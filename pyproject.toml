[project]
name = "agent-fast-paths"
version = "0.1.0"
description = "Reusable fast paths for platform-aware agent web research"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Jeff Green"}]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"
